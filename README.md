# Self-Improving-Agent
An AI agent that automatically improves its own prompt based on output quality scores — runs a task, evaluates the result, rewrites the prompt, and tries again until it hits the target score.
